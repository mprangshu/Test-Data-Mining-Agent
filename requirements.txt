# --- Core agent framework ---
langgraph>=0.2.0
langchain-core>=0.3.0

# --- Parsing ---
# Primary inputs: openpyxl (xlsx test-case sheets); stdlib csv/json; lightweight Gherkin for .txt.
# Supporting inputs: stdlib xml.etree for JUnit XML, native JSON for Playwright (lxml for richer XML).
openpyxl>=3.1.0
lxml>=5.0.0

# --- Failure clustering (spec §2.6 — vector DB, NOT a graph DB) ---
chromadb>=0.5.0

# --- LLM (generate / synthesise seam) ---
# Google Gemini via the google-genai SDK. Key from env GEMINI_API_KEY (never committed).
# Offline default is deterministic generation, so this is only needed to enable LLM output.
google-genai>=0.8.0

# --- Persistence (run store) ---
pymongo>=4.6.0

# --- Demo backend API (Phase 2) ---
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9     # required by FastAPI for multipart/form-data uploads

# --- Dev / test ---
pytest>=8.0.0
httpx>=0.27.0               # FastAPI TestClient transport
