# --- Core agent framework ---
langgraph>=0.2.0
langchain-core>=0.3.0

# --- Parsing (ingest node) ---
# stdlib xml.etree is used in the reference parser; these enable richer/robust parsing.
junitparser>=3.1.0
lxml>=5.0.0

# --- Failure clustering (spec §2.6 — vector DB, NOT a graph DB) ---
chromadb>=0.5.0

# --- Persistence (run store) ---
pymongo>=4.6.0

# --- Dev / test ---
pytest>=8.0.0
